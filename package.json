{
  "name": "globalflows-app",
  "private": true,
  "type": "module",
  "description": "Global macro causal picture \u2014 liquidity \u2192 rates/dollar \u2192 growth/inflation \u2192 risk",
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "ingest:markets": "node scripts/ingest-markets.mjs",
    "markets:live": "node scripts/markets-live.mjs",
    "sanity": "node scripts/sanity.mjs",
    "audit:bands": "node scripts/audit-bands.mjs",
    "audit:calls": "node scripts/audit-calls.mjs",
    "calibrate:lights": "node scripts/calibrate-lights.mjs",
    "calibrate:val": "node scripts/calibrate-val.mjs",
    "verify": "npm run sanity && npm run audit:bands && npm run audit:calls",
    "bake:regime": "node scripts/bake-regime.mjs",
    "bake:report": "node scripts/bake-report.mjs",
    "mint:icons": "python3 scripts/mint-icons.py",
    "fetch:vintages": "node scripts/fetch-vintages.mjs",
    "refresh": "npm run ingest && npm run fetch:vintages && npm run bake:history && npm run calibrate:lights && npm run calibrate:val && npm run bake:regime && npm run sanity",
    "refresh:markets": "npm run ingest:markets && npm run bake:regime",
    "serve": "python3 serve-https.py",
    "bake:history": "node scripts/bake-history.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
